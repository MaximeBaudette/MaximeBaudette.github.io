name: pages build and deployment

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  skip:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ""
    steps:
      - run: echo "Skipped — site is deployed to Cloudflare Pages"
